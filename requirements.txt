# Core async
httpx==0.27.0
websockets==12.0
anyio==4.4.0

# Data validation
pydantic>=2.11.7
pydantic-settings>=2.3.0

# Ethereum / wallet signing
# web3 NO incluido — arrastra pytest-ethereum que rompe pytest con eth_typing
# Solo necesitamos eth-account para firmar EIP-712 localmente
eth-account>=0.13.0

# Data processing
pandas>=2.2.0
numpy>=1.26.0

# Database
aiosqlite==0.20.0
# For postgres (optional):
# asyncpg==0.29.0
# sqlalchemy[asyncio]==2.0.30

# Logging
structlog>=24.2.0

# Environment
python-dotenv==1.0.1

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.7
pytest-cov>=5.0.0
respx>=0.21.1  # mock para httpx

# Web server (dashboard)
aiohttp>=3.9.0

# Utils
tenacity>=8.3.0
rich>=13.7.1
click>=8.1.7
certifi>=2024.2.2  # SSL certs para macOS y Docker
